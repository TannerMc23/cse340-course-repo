<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>
    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>

    <% if (user && user.role_name === 'admin') { %>
        <p><a href="/users">View All Users</a></p>
    <% } %>

    <h3>My Volunteer Projects</h3>
    <% if (volunteeredProjects.length === 0) { %>
        <p>You have not signed up to volunteer for any projects yet.</p>
    <% } else { %>
        <ul>
            <% volunteeredProjects.forEach(project => { %>
                <li>
                    <a href="/project/<%= project.project_id %>"><%= project.title %></a>
                    - <%= project.organization_name %>
                    (<%= project.date.toLocaleDateString() %>)
                    <form action="/unvolunteer/<%= project.project_id %>?from=dashboard" method="POST" style="display:inline;">
                        <button type="submit">Remove</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    <% } %>
</main>
<%- include('partials/footer') %>